---
# Specialized playbook for running llama.cpp tasks
# Usage: ansible-playbook run-llama-cpp.yml

- name: Run llama.cpp installation
  import_playbook: run-with-env.yml
  vars:
    playbook: "playbook-proxmox-lxc.yml"
    tag_list: "llama-cpp"
    enable_llama_cpp: true
